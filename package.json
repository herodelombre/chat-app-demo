{
  "name": "chat-app-example",
  "version": "1.0.0",
  "description": "Demo chat application with React frontend and Node.js backend using polling",
  "main": "index.js",
  "scripts": {
    "install-all": "npm install && cd server && npm install && cd ../client && npm install",
    "start": "npm run build:client && node start-production.js",
    "build:client": "cd client && npm run build",
    "prod": "node start-production.js"
  },
  "devDependencies": {
    "concurrently": "^7.6.0"
  },
  "keywords": [
    "chat",
    "react",
    "nodejs",
    "express",
    "polling",
    "demo"
  ],
  "author": "",
  "license": "MIT",
  "engines": {
    "node": ">=16.0.0"
  }
}
