{
  "name": "chat-app-server",
  "version": "1.0.0",
  "description": "Simple chat application server with in-memory storage",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "start:prod": "npm run prebuild && npm run build:frontend && node server.js",
    "dev": "nodemon server.js",
    "prebuild": "echo '🔨 Preparing production build...'",
    "build:frontend": "cd ../client && npm run build",
    "build:verify": "node -e \"const fs=require('fs'),path=require('path');const buildPath=path.join(__dirname,'..','client','build');if(!fs.existsSync(buildPath)||!fs.existsSync(path.join(buildPath,'index.html'))){console.error('❌ Frontend build failed or incomplete');process.exit(1);}console.log('✅ Frontend build verified');\""
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "keywords": [
    "chat",
    "express",
    "nodejs",
    "polling"
  ],
  "author": "",
  "license": "MIT"
}
